import React from "react";

const Home = () => {
  return (
    <div className='App'>
      <h3>Välkommen till start</h3>
      {/* <input name='searchWord' onChange={handleChange} /> */}
      {/* <button onClick={() => fetchfromApi()}>
        Skrapa data med valt sökord
      </button> */}

      {/* TODO: SPINNER */}
      {/* <Spinner isLoading={isLoading} /> */}
    </div>
  );
};

export default Home;
